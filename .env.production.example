# =============================================================================
# REPLYHQ.AI - PRODUCTION ENVIRONMENT VARIABLES OVERVIEW
# =============================================================================
# This file provides an overview of all environment variables needed for
# production deployment. See individual .env.production.example files in
# backend/ and frontend/ directories for detailed configurations.
# =============================================================================

# =============================================================================
# BACKEND ENVIRONMENT VARIABLES (Set in Railway)
# =============================================================================

# Database (Railway auto-provisions)
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/railway

# Redis Cache (Railway auto-provisions)
REDIS_URL=redis://default:password@host:6379

# Security
SECRET_KEY=generate-with-openssl-rand-hex-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
CORS_ORIGINS=["https://replyhq.ai", "https://www.replyhq.ai"]

# Instagram/Meta Integration
INSTAGRAM_VERIFY_TOKEN=your-webhook-verify-token
INSTAGRAM_APP_SECRET=your-app-secret
INSTAGRAM_ACCESS_TOKEN=your-page-access-token
INSTAGRAM_PAGE_ID=your-page-id

# OpenAI
OPENAI_API_KEY=sk-your-api-key
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_MODEL=gpt-4.1-mini
OPENAI_TEMPERATURE=0.7
OPENAI_MAX_TOKENS=500

# Shopify
SHOPIFY_STORE_URL=your-store.myshopify.com
SHOPIFY_API_KEY=your-api-key
SHOPIFY_ACCESS_TOKEN=shpat_your-token

# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES (Set in Vercel)
# =============================================================================

# API URL (public, exposed to browser)
NEXT_PUBLIC_API_URL=https://api.replyhq.ai

# =============================================================================
# QUICK SETUP CHECKLIST
# =============================================================================
# [ ] Generate SECRET_KEY: openssl rand -hex 32
# [ ] Set up PostgreSQL on Railway
# [ ] Set up Redis on Railway
# [ ] Configure Meta Developer App for Instagram
# [ ] Get OpenAI API key
# [ ] Set up Shopify Admin API access
# [ ] Configure DNS for replyhq.ai
# [ ] Set CORS_ORIGINS to your production domain
