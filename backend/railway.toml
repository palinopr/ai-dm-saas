[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# Railway automatically injects PORT environment variable
# The start command runs migrations before starting the server
startCommand = "alembic upgrade head && uvicorn src.main:app --host 0.0.0.0 --port $PORT"

# Resource configuration (adjust based on your plan)
# numReplicas = 1
